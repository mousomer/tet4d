{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "tet4d/tutorial_lessons.schema.json",
  "title": "tet4d tutorial lessons schema",
  "type": "object",
  "required": [
    "schema_version",
    "lessons"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "minimum": 1
    },
    "lessons": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/lesson"
      }
    }
  },
  "$defs": {
    "stringList": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "ui": {
      "type": "object",
      "required": [
        "text",
        "highlights",
        "key_prompts"
      ],
      "properties": {
        "text": {
          "type": "string",
          "minLength": 1
        },
        "hint": {
          "type": [
            "string",
            "null"
          ]
        },
        "highlights": {
          "$ref": "#/$defs/stringList"
        },
        "key_prompts": {
          "allOf": [
            {
              "$ref": "#/$defs/stringList"
            },
            {
              "minItems": 1,
              "maxItems": 1
            }
          ]
        }
      }
    },
    "gating": {
      "type": "object",
      "required": [
        "allow",
        "deny"
      ],
      "properties": {
        "allow": {
          "$ref": "#/$defs/stringList"
        },
        "deny": {
          "$ref": "#/$defs/stringList"
        }
      }
    },
    "setup": {
      "type": "object",
      "properties": {
        "camera_preset": {
          "type": "string",
          "minLength": 1
        },
        "spawn_piece": {
          "type": "string",
          "minLength": 1
        },
        "starter_piece_id": {
          "type": "string",
          "minLength": 1
        },
        "board_preset": {
          "type": "string",
          "minLength": 1
        },
        "rng_seed": {
          "type": "integer"
        },
        "spawn_min_visible_layer": {
          "type": "integer",
          "minimum": 0
        },
        "bottom_layers_min": {
          "type": "integer",
          "minimum": 0
        },
        "bottom_layers_max": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "completeWhen": {
      "type": "object",
      "required": [
        "events",
        "predicates",
        "logic"
      ],
      "properties": {
        "events": {
          "allOf": [
            {
              "$ref": "#/$defs/stringList"
            },
            {
              "minItems": 1,
              "maxItems": 1
            }
          ]
        },
        "predicates": {
          "$ref": "#/$defs/stringList"
        },
        "logic": {
          "type": "string",
          "enum": [
            "all",
            "any"
          ]
        },
        "event_count_required": {
          "type": "integer",
          "minimum": 1,
          "maximum": 9
        }
      }
    },
    "step": {
      "type": "object",
      "required": [
        "id",
        "ui",
        "gating",
        "setup",
        "complete_when"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "ui": {
          "$ref": "#/$defs/ui"
        },
        "gating": {
          "$ref": "#/$defs/gating"
        },
        "setup": {
          "$ref": "#/$defs/setup"
        },
        "complete_when": {
          "$ref": "#/$defs/completeWhen"
        }
      }
    },
    "lesson": {
      "type": "object",
      "required": [
        "lesson_id",
        "title",
        "mode",
        "steps"
      ],
      "properties": {
        "lesson_id": {
          "type": "string",
          "minLength": 1
        },
        "title": {
          "type": "string",
          "minLength": 1
        },
        "mode": {
          "type": "string",
          "enum": [
            "2d",
            "3d",
            "4d"
          ]
        },
        "steps": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/step"
          }
        }
      }
    }
  }
}
