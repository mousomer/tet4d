name: Apply Slice C mapping reference

on:
  push:
    branches:
      - codex/path-sensitive-ci-lanes

permissions:
  contents: write

concurrency:
  group: apply-slice-c-mapping-${{ github.ref }}
  cancel-in-progress: true

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: codex/path-sensitive-ci-lanes
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]

      - name: Regenerate and verify the offline mapping boundary
        run: |
          python tools/governance/generate_configuration_reference.py
          python -m ruff format tools/governance/select_codex_ci_lanes.py tests/unit/governance/test_select_codex_ci_lanes.py
          python -m ruff check tools/governance/select_codex_ci_lanes.py tests/unit/governance/test_select_codex_ci_lanes.py
          python -m pytest -q tests/unit/governance/test_select_codex_ci_lanes.py
          git diff --check

      - name: Commit permanent final tree
        run: |
          rm .github/workflows/apply_slice_c_mapping.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Regenerate Codex CI lane mapping reference"
          git push origin HEAD:codex/path-sensitive-ci-lanes
